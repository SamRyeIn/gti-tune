That is the full workflow: import and preflight, edit in physical units, review the journal, build through the verification gate, export only the verified file, then use datalogs from the real car to begin the next revision. The app keeps the session recoverable, declares no network or vehicle permissions, and never flashes an ECU. Preserve the original recovery bin, review every candidate, and let evidence from the car—not the existence of a green Build screen—decide whether the calibration is ready.
