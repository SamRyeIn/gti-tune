Full-load enrichment edits IP_LAMB_FL_SP — Lambda setpoint at full load. Each chip selects a time row after full load begins, and the graph shows lambda versus engine speed. Lower lambda is richer. Choose the time row, then drag a point, tap for exact entry, or flatten the row to one value.

The shaded band near lambda one warns that the request provides little or no enrichment. The engine also reports a lean refusal boundary; a typed value at or beyond it is rejected, while a drag stops at the highest settable value. The screen calls out a completely unenriched row because it deserves deliberate review. Work through the time rows, record your intent, and Apply the staged row once. Use Discard if the shape is not what you meant. Verification proves the file matches the request; only logs can show whether the fueling result is healthy.
